# Развернуть Managed PostgreSQL в Yandex Cloud

## Создать кластер Ресурсы: 1 vCPU, 1 ГБ RAM

Таких параметров нет в доступе, выбираем другие минимальные. Разрешаем авто-ресайз хранилища.
![[Pasted image 20260127201859.png]]

![[Pasted image 20260127201942.png]]

## Разрешить доступ с вашего IP

Выбираем дефолтную группу, в ней доступ из ЯО разрешен (через Группы безопасности).
![[Pasted image 20260127202025.png]]

## Подключиться через psql Проверить работоспособность кластера

Подключаемся с использованием данных из консоли, проверяем, доступ есть.

Хосты PostgreSQL с публичным доступом поддерживают только шифрованные соединения. Чтобы использовать их, получаем SSL-сертификат:
```
mkdir -p ~/.postgresql && \
wget "https://storage.yandexcloud.net/cloud-certs/CA.pem" \ 
--output-document ~/.postgresql/root.crt && 
\ chmod 0655 ~/.postgresql/root.crt
```

Ставим зависимости
```
sudo apt update && sudo apt install --yes postgresql-client
```

По умолчанию базы данных в Yandex Cloud создаются внутри закрытой сети (VPC) и недоступны из интернета.

```
romany4@minikube-1:~$ psql "host=rc1b-4rduiu7o95cec8r0.mdb.yandexcloud.net \
    port=6432 \
    sslmode=verify-full \
    dbname=user1 \
    user=user1 \
    target_session_attrs=read-write"
psql: error: could not translate host name "rc1b-4rduiu7o95cec8r0.mdb.yandexcloud.net" to address: Name or service not known
```

- **Решение:** В консоли Yandex Cloud выбираем кластер PostgreSQL, переходим на вкладку **«Хосты»** и проверяем, что для нужного хоста включена опция **«Публичный доступ»**.

Проверяем
```
romany4@minikube-1:~/.postgresql$ psql "host=rc1b-4rduiu7o95cec8r0.mdb.yandexcloud.net,rc1b-e53oqjjmsqioahaf.mdb.yandexcloud.net \
    port=6432 \
    sslmode=verify-full \
    dbname=db1 \
    user=user1 \
    target_session_attrs=read-write"
Password for user user1:
psql (17.7 (Debian 17.7-0+deb13u1), server 16.10 (Ubuntu 16.10-201-yandex.57618.c888a668ba))
SSL connection (protocol: TLSv1.3, cipher: TLS_AES_256_GCM_SHA384, compression: off, ALPN: none)
Type "help" for help.
```

```
db1=> SELECT version();
                                                                      version
---------------------------------------------------------------------------------------------------------------------------------------------------
 PostgreSQL 16.10 (Ubuntu 16.10-201-yandex.57618.c888a668ba) on x86_64-pc-linux-gnu, compiled by gcc (Ubuntu 11.4.0-1ubuntu1~22.04) 11.4.0, 64-bit
(1 row)
```